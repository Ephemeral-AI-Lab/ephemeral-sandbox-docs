/goal Wire every frozen HV-07 faultpoint into its corresponding real durable operation and prove host-side completeness without claiming physical crash evidence.

You are M2R Worker B. Begin by reading the current `/goal` objective file supplied to your task, all repository rules, the Stage 04.6 governing requirements/plan/test matrix/implementation plan/progress tracker, and the complete lead assignment capsule.

Do not edit until the capsule supplies:

- exact corrective checkpoint SHA/ref;
- frozen `m2r-iface-v1` or an exact lead-owned additive commit to consume;
- exact exclusive file paths;
- corrective run/evidence root;
- current B-007 capsule;
- physical execution lease status.

If any item or ownership boundary is missing, report `NEEDS_ATTENTION` precisely. Do not edit shared contracts, the named fault registry, manifests, aggregate dispatch, tracker, security profile, or paths not explicitly transferred.

## Scope

The prior aggregate implementation accounted for all 46 names but only 10 child paths executed a corresponding real `PublicationRecovery` operation; 36 called the marker directly. Correct that evidence gap.

For every entry in `NamedFaultPoint::ALL`:

- map it to the exact real allocation, lease, owner, semantic, locator, ref, activation, projection, evacuation, or retirement operation;
- place the fault boundary at the defined durable edge inside that operation;
- record a real-operation witness distinguishable from merely reaching the marker;
- preserve the old-or-complete-new recovery oracle and exact-owner/locator/ref invariants;
- retain the failed attempt bundle and replay result;
- reject physical-complete status if the real-operation witness, kill witness, recovery witness, or terminal invariant is missing.

A loop that invokes 46 marker names is not sufficient. A fake mutation, mock crash, in-process panic, or precomputed terminal state is not sufficient.

Process-SIGKILL coverage must use real child processes in the eventual physical campaign. If the supported public lifecycle still cannot perform container kill/recreate, keep that subset explicitly `UNKNOWN` and report the exact missing API; do not simulate or silently downgrade it.

## Host verification

Add focused tests that prove:

1. the frozen registry is covered exactly once with no missing or duplicate mapping;
2. 46/46 mappings invoke the intended real operation;
3. each injected edge is reachable only at its real durable boundary;
4. replay yields old-or-complete-new visibility and one exact owner/ref/locator as applicable;
5. incomplete, forged, marker-only, or missing-witness attempts cannot be sealed as physical complete;
6. failed/cancelled/crash bundles are durable and idempotent;
7. no recovery path moves/copies the stationary payload or hides debt/leaks.

Pure host/fault tests and compilation do not need a physical lease. Do not create Docker resources, send SIGKILL to a physical campaign, or claim any HV-07 physical point unless the lead gives the exact point-specific lease.

Run formatting, focused crash/recovery tests, canonical relevant host tests, strict scoped Clippy, and the supported Linux/arm64 build. Preserve and report every failed development command.

## Coordination

Send concise checkpoints to the lead:

- `STARTED`
- `DISCOVERY_COMPLETE`
- `FIRST_BUILD`
- `FOCUSED_TESTS`
- `NEEDS_ATTENTION` when blocked
- `HANDOFF_READY`

Request a lead-owned interface or registry change rather than editing shared files. Commit only exact assigned paths and preserve unrelated worktree changes.

Your canonical handoff must include:

```yaml
phase: M2R
worker: M2R-B HV-07 real-operation wiring
thread_id:
host_id:
outcome: PASS | FAIL | BLOCKED
base_phase_checkpoint_sha:
base_phase_checkpoint_ref:
worker_commit_sha:
interface_version_consumed: m2r-iface-v1
files_changed:
commands:
tests:
resource_maxima:
artifacts:
failures_and_unknowns:
requested_lead_changes:
recommended_next_action:
```

State explicitly that host 46/46 mapping PASS does not constitute process-SIGKILL or container-kill physical PASS. The lead owns aggregate integration, leases, public CLI execution, evidence sealing, and the authoritative HV-07 verdict.
